name: Test Cron Trigger

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: ✅ Afficher un message
        run: echo "🎯 Cron bien déclenché"
